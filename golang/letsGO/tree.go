package main

import "fmt"

type Tree struct {
	value       int
	left, right *Tree
}

func AddNode(t *Tree, value []int) {
	if t == nil {
		t = new(Tree)

	}
}

func Sort(value []int) *Tree {
	var root *Tree
	return root
}

func main() {
	var t Tree
	fmt.Println(t)
}
